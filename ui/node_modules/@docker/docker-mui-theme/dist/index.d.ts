import React from 'react';
import type { ReactNode } from 'react';
import type { ThemeOptionsBag } from './ThemeOptionsBag';
interface Props {
    readonly children: ReactNode;
    /**
     * Server Side Render flag. Set this prop to `true` when you are doing CSR and set it to `false` when doing SSR. Defaults to `true` (Client Side Rendering).
     */
    noSsr?: boolean;
}
/** @deprecated Use either `DockerMuiV5ThemeProvider` or `DockerMuiV6ThemeProvider` */
export declare const DockerMuiThemeProvider: typeof DockerMuiV5ThemeProvider;
export declare function DockerMuiV5ThemeProvider({ children, noSsr }: Props): React.JSX.Element;
export declare function DockerMuiV6ThemeProvider({ children, noSsr }: Props): React.JSX.Element;
declare global {
    var __ddMuiV5Themes: ThemeOptionsBag;
    var __ddMuiV6Themes: ThemeOptionsBag;
}
export {};
